CONSTANTS
    u1 = u1
    u2 = u2
    admin = admin
    member = member
    r1 = r1
    read = read
    write = write
    p1 = p1
    p2 = p2
    Users = {u1, u2}
    Roles = {admin, member}
    Resources = {r1}
    Permissions = {read, write}
    Policies = {p1, p2}

INIT Init
NEXT Next

CONSTRAINT StateConstraint

INVARIANT SafetyInvariant
